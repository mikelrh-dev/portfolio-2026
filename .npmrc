# @react-three/postprocessing@3 declares peer fiber@^9 while the project
# pins fiber@^8 — resolution works in practice, so relax npm's strict
# peer check to match the local environment.
legacy-peer-deps=true
